// Developer config setter placeholder (text-mode removed).

module.exports = {
  name: 'devset',
  description: 'Developer-only: set value in config file — text mode removed; use /devmenu',
  developerOnly: true
};
