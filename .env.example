# Copy this file to .env and fill values

# Bot tokens (never commit real tokens)
# For public (hosted) bot, set the public token in your host environment or here for local testing
TOKEN=
# Optional: profile-specific token variable name referenced by config/bot.dev.json or bot.public.json
# e.g. TOKEN_DEV or TOKEN_PUBLIC

# Optional client id (used by deploy scripts)
CLIENT_ID=

# Guild to use for fast local registration (dev)
GUILD_ID=

# Database (Postgres) URL; if unset, development uses local sqlite
DATABASE_URL=

# Logging
LOG_LEVEL=info
# Force ANSI color codes in consoles that don't expose a TTY (set to 1 to enable)
LOG_FORCE_COLOR=1

# Deployment helpers
# Allow global registration when running deploy-commands (set on CI/host when you intend global registration)
ALLOW_GLOBAL_REGISTRATION=false
# Auto-run public global deploy on host startup (opt-in; use with caution)
AUTO_DEPLOY_PUBLIC=false
# Auto-run dev guild deploy on local startup (opt-in)
DEV_AUTO_DEPLOY=false

# Sentry (optional)
SENTRY_DSN=
## Production / Public bot token (used by `npm start`)
TOKEN=your-public-bot-token

## Development / Private bot token (used by `npm run start:dev`)
## Keep this separate from the production token and do NOT commit real tokens.
TOKEN_DEV=your-dev-bot-token

## Optional override to pick a bot profile explicitly (values: "public" or "dev")
## If set, `BOT_PROFILE` takes precedence over NODE_ENV when selecting bot.*.json
BOT_PROFILE=dev

CLIENT_ID=your-application-client-id
GUILD_ID=your-dev-guild-id   # optional; if set, commands deploy to this guild for faster testing
DATABASE_URL=                    # optional: e.g. postgres://user:pass@host:5432/dbname
SENTRY_DSN=                       # optional: Sentry DSN to send errors
PAPERTRAIL_HOST=                   # optional: host for Papertrail logs
PAPERTRAIL_PORT=                   # optional: port for Papertrail logs
LOG_LEVEL=info                     # optional: debug|info|warn|error
